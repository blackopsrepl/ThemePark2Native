#pragma once

// Windows resources use small integer identifiers.  Both the executable icon
// and the window class refer to this same number, so Explorer, the taskbar,
// Alt+Tab, and the title bar all display the same artwork.
#define IDI_THEMEPARK_NATIVE 101
